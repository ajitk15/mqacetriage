# =============================================================================
# LEGACY: Unified IBM MQ + IBM ACE MCP Server — root-level environment
#
# ⚠️  Each app now has its own .env.example. Copy and configure these instead:
#
#   mqacemcpserver-single/.env.example  →  mqacemcpserver-single/.env
#   backend/.env.example                →  backend/.env
#   frontend/.env.example               →  frontend/.env
#   dashboard/.env.example              →  dashboard/.env
#
# This root .env.example is kept for reference only.
# =============================================================================

# See each app's .env.example for the full variable reference.
